<div class="form-group">
  <label for="exampleFormControlSelect3">What is it?</label>
  <select class="form-control" id="response_received<%= a.id %>">
    <option selected="">Acknowledgement/confirmation</option>
    <option>Request of clarification</option>
    <option>We can't verify your identity</option>
    <option>We don't know you</option>
    <option>Some personal data</option>
    <option>No data</option>
    <option>Invite to phone call</option>
    <option>Invite to visit</option>
    <option>Request to be more specific</option>
    <option>Other</option>
  </select>
</div>
<div class="form-group">
  <label for="exampleFormControlTextarea1">Remark</label>
  <textarea class="form-control" id="responseRemarks<%= a.id %>" rows="4"></textarea>
</div>
<div class="form-group">
  <label for="labelFileUpload<%= a.id %>">Upload attachment(s)</label>
  <input type="file" class="form-control-file" id="fileUpload<%= a.id %>">
</div>
<label class="form-check-label">
  <input class="form-check-input" type="checkbox" value="">
  I don't want to upload an attachment.
  <small>(You can always do it later.)</small>
</label>
